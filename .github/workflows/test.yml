name: Test

on:

  push:

    branches:

      - main

jobs:

  test:

    runs-on: ubuntu-latest

    steps:

    - uses: actions/checkout@v2
    
    - name: Install dependencies

      run: php install

    - name: Run tests

      run: php test
